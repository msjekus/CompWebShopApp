@using CompWebShopDomainLibrary
@model IEnumerable<Category>
@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@
@{ 
	SelectList ParentCategories = new SelectList(
		Model,
		nameof(Category.Id),
		nameof(Category.CategoryName));
}
<div class="form-group">
    <label for="parentCategoryId" class="control-label"></label>
    <select name="parentCategoryId[]" class="form-control" asp-items="ParentCategories">
		<option value="@(null)">Немає</option>
	</select>
    
</div>